# 低代码平台重构回归测试用例清单 (Manual Regression Test Cases)

- **适用分支**：`refactor/lowcode-directory-structure`
- **测试环境地址**：`http://localhost:8080/vpages/#/`
- **测试账号密码**：`<您的测试账号>` / `<您的测试密码>`

---

## 模块 1：环境准备与登录鉴权

### 【TC-01】自动重定向与登录鉴权
* **测试目的**：验证未登录状态下访问受保护页面能正常弹出登录框，输入正确凭据后能顺利登录。
* **操作步骤**：
  1. 打开浏览器无痕模式，访问 `http://localhost:8080/vpages/#/site/BX2506130908230001`。
  2. 观察页面是否弹出全屏登录弹窗。
  3. 输入正确的用户名与密码，点击【登录】。
* **预期结果**：
  - 登录提示成功，弹窗消失；
  - 页面自动加载出高新技术产业开发区官网主页，首屏 Banner、导航栏与底层卡片展示正常。

---

## 模块 2：PC 端官网渲染与导航切换测试

### 【TC-02】高新官网主页渲染 (BX2506130908230001)
* **测试目的**：验证 PC 官网主页的低代码布局、大图轮播与部件复用。
* **访问地址**：`http://localhost:8080/vpages/#/site/BX2506130908230001`
* **重点检查项**：
  1. **Logo 与导航栏**：左上角 Logo 图片加载顺畅无白屏闪烁；
  2. **页面平滑**：滚动页面无横向抖动（滚动条固定槽位 `scrollbar-gutter` 生效）；
  3. **数据卡片**：新闻列表、通知公告卡片数据完整展示。

### 【TC-03】走进园区官网页渲染 (PG2505171818300004)
* **测试目的**：验证子页面路由切换与 SWR 快速加载。
* **访问地址**：`http://localhost:8080/vpages/#/site/PG2505171818300004`
* **重点检查项**：
  1. 页面内容无缝切换，顶部导航栏无需重建重新加载；
  2. 园区介绍、地图部件渲染正常。

### 【TC-04】顶部导航栏点击交互
* **测试目的**：验证点击顶部 Header 导航菜单的路由派发与视图刷新。
* **操作步骤**：
  1. 在官网页面顶部导航栏中，依次点击【走进高新】、【新闻中心】、【走进园区】等菜单。
* **预期结果**：
  - URL 路径平滑切换，组件树保持复用，页面无全屏闪白块；
  - F12 控制台（Console）无 Module 报错。

---

## 3. 模块 3：旧路径向后兼容性测试

### 【TC-05】旧路由 `/lowcode/view/:pageNo` 兼容性
* **测试目的**：验证通过重构前的旧 URL 路径访问时，代理转发组件生效。
* **访问地址**：`http://localhost:8080/vpages/#/lowcode/view/PG2505171818300004`
* **预期结果**：
  - 页面自动通过 `LowCodeViewProxy` 代理正确显示园区官网，内容与功能与全新路由一致；
  - 浏览器控制台零 404 错误。

---

## 4. 模块 4：PC 端低代码可视化编辑器测试

### 【TC-06】PC 端低代码编辑器画布与属性面板
* **测试目的**：验证可视化编辑器的物理拖拽、属性编辑与样式配置能力。
* **访问地址**：`http://localhost:8080/vpages/#/lowcode/editor/PG2505171818300004`
* **操作步骤**：
  1. 观察左侧组件大纲树、中间可视化预览画布、右侧属性配置面板是否正常加载。
  2. 点击画布上的任意卡片部件，检查右侧属性面板是否正确高亮对应的字段配置。
  3. 切换右侧【样式配置】与【属性配置】Tab。
* **预期结果**：
  - 编辑器三栏布局加载流畅，右侧属性与样式 Tab 切换无 `TypeError` 报错；
  - 页面顶部的【预览】与【保存】按钮可正常点击。

---

## 5. 模块 5：移动端编辑器与预览测试 (low-app)

### 【TC-07】移动端拖拽编辑器 (`/app/edit/:pageNo`)
* **测试目的**：验证从 `low-app` 归集至 `lowcode/editor/mobile` 后的移动端编辑器。
* **访问地址**：`http://localhost:8080/vpages/#/app/edit/PG2505171818300004`
* **预期结果**：
  - 中间移动端手机模拟器框正常呈现；
  - 右侧组件属性编辑面板正常加载。

### 【TC-08】移动端独立预览页面 (`/app/preview/:pageNo`)
* **测试目的**：验证移动端 H5 预览页面的独立渲染。
* **访问地址**：`http://localhost:8080/vpages/#/app/preview/PG2505171818300004`
* **预期结果**：
  - 呈现标准的移动端 H5 布局形态，页面卡片内容完整。
