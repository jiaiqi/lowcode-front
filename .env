# 通用构建配置（提交到 git，团队共用）
VUE_APP_BASE_URL = /vpages
VUE_APP_ROUTE_MODE = hash

# 默认环境（延安园区开发）；个人覆盖用 .env.local 的 VUE_APP_DEFAULT_ENV
VUE_APP_DEFAULT_ENV = parkDev

# 开启全局左上角搜索框（默认关闭，个人覆盖请在 .env.local 中设置 VUE_APP_ENABLE_LIST_GLOBAL_SEARCH）
VUE_APP_ENABLE_LIST_GLOBAL_SEARCH = false
